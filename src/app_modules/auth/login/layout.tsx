"use client";

import {
  AppShell,
  Center,
  Footer,
  Header,
  Image,
  Paper,
  Text,
  Title,
} from "@mantine/core";
import React from "react";

export default function LayoutLogin({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>
  {children}</>;
}
